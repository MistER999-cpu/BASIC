Drop the three product photos here (any filenames), then run:

    python3 tools/ingest.py assets/incoming

They are matted off their backdrop and sorted into white / black / brown by the
garment's own colour, with one shared crop so the product cannot shift between
colourways as a reel spins.
